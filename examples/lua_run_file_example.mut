putln("=== lua_run_file example ===");
putln("requires capability: filesystem");

let result = lua_run_file("examples/lua_sample_patch.lua");

if (result["ok"]) {
    putln("lua_run_file ok");
    putln("result: ", result["result"]);
} else {
    putln("lua_run_file failed: ", result["error"]);
};