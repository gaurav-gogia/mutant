[package]
name = "mutant_rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "mutant_rust"
crate-type = ["staticlib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
